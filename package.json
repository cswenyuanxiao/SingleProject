{
  "name": "quickstart",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "pkill -f 'hugo server' || true && sleep 1 && hugo server --disableFastRender --port=1313 --bind=127.0.0.1",
    "build": "hugo",
    "deploy": "bash deploy-github.sh",
    "deploy:netlify": "hugo && netlify deploy --prod"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@filipecarneiro/hugo-bootstrap-theme": "^1.0.49"
  }
}
