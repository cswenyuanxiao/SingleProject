# Disabled GitHub Actions workflow
name: DISABLED - Hugo site deployment

on:
  # Only triggered manually, effectively fully disabled
  workflow_dispatch:
  # push:
  #   branches: ["main"]

# Content retained for reference only
# permissions:
#   contents: read
#   pages: write
#   id-token: write

# concurrency:
#   group: "pages"
#   cancel-in-progress: true

jobs:
  # Empty job, performs no operations
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: This workflow is disabled
        run: echo "This workflow has been disabled" && exit 0 