# 已禁用的 GitHub Actions 工作流
name: DISABLED - Hugo site deployment

on:
  # 仅在手动触发时执行，实际上已完全禁用
  workflow_dispatch:
  # push:
  #   branches: ["main"]

# 以下内容保留仅作参考
# permissions:
#   contents: read
#   pages: write
#   id-token: write

# concurrency:
#   group: "pages"
#   cancel-in-progress: true

jobs:
  # 空作业，不执行任何操作
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: This workflow is disabled
        run: echo "This workflow has been disabled" && exit 0 